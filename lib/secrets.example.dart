// Copy this file to secrets.dart and fill in your own keys.
// secrets.dart is gitignored so the real keys never reach the repo.

const geminiApiKey     = '';
const elevenLabsApiKey = '';
// "Aria" multilingual. Not a secret, but it lives here so all three stay together.
const elevenVoiceId    = 'EXAVITQu4vr4xnSDxMaL';
